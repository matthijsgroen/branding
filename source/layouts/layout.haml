!!!
%html
  %head
    %meta(charset="utf-8")

    / Always force latest IE rendering engine or request Chrome Frame
    %meta(content="IE=edge,chrome=1" http-equiv="X-UA-Compatible")

    / Use title if it's in the page YAML frontmatter
    %title= data.me.profile.name

    = stylesheet_link_tag "normalize", "all"
    = javascript_include_tag  "all"

  %body
    %header
      %img.gravatar{ src: gravatar_url(data.me.profile.email) }
      %h1= I18n.t 'hello', name: %(<span class="name">#{data.me.profile.name}</span>)

    = yield

    %footer
      - if respond_to? :links and !links.empty?
        %h3= I18n.t 'links'
        %ul.links(role="links")
          - links.each do |link|
            %li
              %a{ href: link[:target] }= link[:label]
